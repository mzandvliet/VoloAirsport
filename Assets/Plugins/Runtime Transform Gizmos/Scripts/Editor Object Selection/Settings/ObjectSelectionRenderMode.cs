namespace RTEditor
{
    /// <summary>
    /// Identifies different way in which the object selection can be rendered.
    /// </summary>
    public enum ObjectSelectionRenderMode
    {
        /// <summary>
        /// A selection box is rendered for each selected object.
        /// </summary>
        SelectionBoxes = 0
    }
}
